apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: vault-issuer-ca
spec:
  sources:
    - secret:
        name: vault-issuer-ca
        key: "ca.crt"
  target:
    configMap:
      key: "ca.crt"